# ==============================================================================
# Target:  CUDA 12.8  (cu128)
# Python:  3.11  ← recommended (see note below)
#
# Install order (server):
#   python3.11 -m venv .venv
#   source .venv/bin/activate
#   pip install --upgrade pip
#   pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt
#
# Why Python 3.11:
#   - torch 2.11 cu128 wheels ship for 3.10–3.14
#   - fairscale 0.4.13 (last release 2023) is only tested on 3.11
#   - timm, pytorch-lightning, transformers all have stable 3.11 wheels
#   - 3.12+ breaks fairscale's C-extension build on some servers
# ==============================================================================

# Tell pip where to find CUDA 12.8 wheels as a fallback
--extra-index-url https://download.pytorch.org/whl/cu128

# -------- Core utilities --------
numpy>=1.26,<2.0
pandas>=2.1
einops>=0.7
Pillow>=10.0
matplotlib>=3.8
tqdm>=4.66

# -------- Image & data processing --------
opencv-python>=4.8
pydicom>=2.4
h5py>=3.10
scikit-learn>=1.3
neo4j>=5.0

# -------- Deep learning (CUDA 12.8) --------
torch==2.11.0
torchvision==0.26.0
timm>=0.9

# -------- Training framework --------
pytorch-lightning>=2.4,<3.0
torchmetrics>=1.3
wandb>=0.17

# -------- NLP / multimodal --------
transformers>=4.40,<5.0

# -------- Distributed / gradient checkpointing --------
# NOTE: fairscale 0.4.13 is not actively maintained.
# If it fails to build on your server run:
#   pip install fairscale --no-build-isolation
fairscale>=0.4.13

# -------- Agentic training loop (optional) --------
anthropic>=0.40
